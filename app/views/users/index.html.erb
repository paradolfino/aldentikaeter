<h1>Current Users</h1>
<ul>
    <% @users.each do |user| %>
        <li><%= user.first_name %> <%= link_to '[View]', user_path(user) if current_user && current_user == user %></li>
    <% end %>
</ul>

